# vault — macOS Keychain integration with env var fallback
#
# Named vault (not secrets) to avoid shadowing Python's stdlib
# secrets module — same pattern as apple_calendar/ for the calendar module.
