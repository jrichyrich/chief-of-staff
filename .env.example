# Chief of Staff (Jarvis) — Environment Variables
# Copy to .env and fill in your values.

# Required
ANTHROPIC_API_KEY=

# Project paths (defaults shown — override if your layout differs)
# JARVIS_PROJECT_DIR=$HOME/Documents/GitHub/chief_of_staff
# JARVIS_DATA_DIR=$JARVIS_PROJECT_DIR/data

# iMessage inbox monitor
# JARVIS_DEFAULT_EMAIL_TO=you@example.com
# JARVIS_IMESSAGE_SELF=+15551234567

# Microsoft 365 bridge (optional — only if using M365 connector)
# CLAUDE_BIN=claude
# CLAUDE_MCP_CONFIG=
# M365_BRIDGE_MODEL=sonnet
# M365_BRIDGE_TIMEOUT_SECONDS=90
# M365_BRIDGE_DETECT_TIMEOUT_SECONDS=5

# Calendar routing
# CALENDAR_REQUIRE_DUAL_READ=true

# OneDrive backup (optional — only if using jarvis-backup.sh)
# JARVIS_ONEDRIVE_BASE=$HOME/Library/CloudStorage/OneDrive-YourOrg
